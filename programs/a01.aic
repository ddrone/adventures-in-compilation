# This is a line comment to check whether parsing of those works
function simple(x, y) {
  z = x;
  t = y;
  return t;
}

/*
  This is a block comment,
  also need to check whether it works.
*/
function main() {
  t = 10;
  a = 20;
  return simple(a, t);
}

function opTest() {
  t = 10;
  a = 20;
  b1 = t <= a;
  b2 = t > a;
  b3 = b1 && b2;
  b4 = b1 || b2;
  return b4;
}

function ifTest(x) {
  if (y) {
    return 0;
  }
}
