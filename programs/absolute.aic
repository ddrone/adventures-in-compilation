function absolute(x) {
  if (x < 0) {
    return -x;
  } else {
    return x;
  }
}

function twiceAbsolute(x) {
  if (x < 0) {
    y = -x;
  }
  else {
    y = x;
  }
  return 2 * y;
}

function main() {
  return absolute(5) + absolute(-10);
}
