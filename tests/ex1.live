block_0:
  ∅
    movq $12, x
  {x}
    addq $20, x
  {x}
    movq $10, @0
  {x, @0}
    addq x, @0
  {@0}
    movq @0, %rdi
  {%rdi}
    callq print_int
  ∅
    movq $0, %rax
  ∅
    jmp conclusion
  ∅

conclusion:
  ∅

