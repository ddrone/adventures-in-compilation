block_0:
  ∅
    callq read_int
  {%rax}
    movq %rax, x
  {x}
    callq read_int
  {%rax, x}
    movq %rax, y
  {x, y}
    movq x, @0
  {y, @0}
    addq y, @0
  {@0}
    movq @0, %rdi
  {%rdi}
    callq print_int
  ∅
    movq $0, %rax
  ∅
    jmp conclusion
  ∅

conclusion:
  ∅

