block_0:
  {$0}
    callq read_int
  {$0, %rax}
    movq %rax, x
  {$0, x}
    callq read_int
  {$0, %rax, x}
    movq %rax, y
  {$0, x, y}
    movq x, @0
  {$0, y, @0}
    addq y, @0
  {$0, @0}
    movq @0, %rdi
  {$0, %rdi}
    callq print_int
  {$0}
    movq $0, %rax
  ∅
    jmp conclusion
  ∅

conclusion:
  ∅

